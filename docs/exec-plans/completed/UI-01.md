# UI-01 — V1.1 Enterprise Visual Foundation

Status: `PASS_WITH_NOTES`

Execution stage: `EXTERNALLY_ACCEPTED`

## Scope

- CR-UI-01 approved; `V1.1 / FROZEN — Enterprise Visual Experience Upgrade` is the active visual baseline.
- Establish one shared semantic design-token system, light/dark theme, global shell, abstract brand mark, ambient field, and first-session opening experience.
- Apply the foundation to Admin and Customer shells/login without redesigning every page or changing business behavior.
- Add focused unit and Playwright coverage; introduce no runtime dependency.

## Implementation

- Added early HTML theme resolution, shared `ThemeProvider`/`ThemeToggle`, safe preference persistence, and system-preference fallback.
- Added semantic OKLCH colors, layered surfaces, typography, radii, shadows, gradients, scrollbars, focus treatment, and a limited dark compatibility bridge for UI-02-owned Admin interiors.
- Added reusable shell primitives, CSS-only ambient effects, an SVG Star + data-grid + AI-core identity, page-content motion, and a 2.3-second deterministic session opening with a 180ms reduced-motion path.
- Updated Admin/Customer shells and shared UI primitives while preserving auth, session, CSRF, SSE, WebSocket, API, routing, permission, and business contracts.

## Verification

- `npm --prefix frontend ci`
- `npm --prefix frontend run format:check`
- `npm --prefix frontend run lint`
- `npm --prefix frontend run test`
- `npm --prefix frontend run build`
- `npm --prefix frontend run test:e2e`
- Browser screenshots reviewed for Customer/Admin light and dark themes plus the opening frame; screenshots remain in ignored Playwright output.

## Boundaries

- No backend, migration, API, auth, Agent/RAG, workflow, or product behavior change.
- No new runtime dependency, canvas/WebGL renderer, continuous JavaScript animation loop, large asset, PR, or merge.
- Externally accepted `PASS_WITH_NOTES` at `5c70565db2985fa943314a3f69d72948ec65dee8`.
- UI-02 continues on the accepted integration branch; UI-03 remains gated.
