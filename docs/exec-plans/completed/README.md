# Completed Execution Plans

Externally accepted task plans are moved here from [`../active/`](../active/). Keep them Git-tracked and do not delete historical plans. A plan may be moved only after the acceptance owner explicitly records `PASS` or `PASS_WITH_NOTES` in the project state and roadmap.
