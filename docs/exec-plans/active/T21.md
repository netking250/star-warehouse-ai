# T21 - Final Integration, Evaluation, and Portfolio Readiness

## Status

- Task: `T21`.
- Status: `IN_PROGRESS`.
- Execution stage: `IMPLEMENT_BLOCKED`.
- Branch: `feat/t14-t21-enterprise-hardening`.
- Recovery head: `d7acd74d0fddc9b4b539c8c8300c37617660b1d4`.
- Evidence-pass head: `540ea8c0c889c183050b7948bbdb07fac98e4eef`.
- T20 prerequisite: externally accepted `PASS_WITH_NOTES` by the explicit T21 instruction.

## Frozen scope

Integrate and evaluate the accepted T14-T20 work, run the one final local regression pass, and
prepare truthful reviewer/interview evidence for one later final pull request. Reuse the existing
evaluation, CI, deployment, observability, and runbook systems. Do not add a business feature,
provider architecture, infrastructure stack, database, authentication scheme, queue, production
HA/PITR implementation, live cloud deployment, release, pull request, or merge.

## Recovery

- T14 and T18-T20 are externally accepted `PASS_WITH_NOTES`; T15-T17 are externally accepted
  `PASS`; T21 began from `NOT_STARTED`.
- The worktree was clean and local `feat/t14-t21-enterprise-hardening` matched its remote at the
  recovery head.
- Alembic must remain a single immutable head at `e9f0a1b2c3d4`; no migration is expected.
- Existing evaluation code lives in `app/evaluation`, `tests/evaluation`,
  `data/golden_dataset_v2.jsonl`, `scripts/run_evaluation.py`, and `.github/workflows/eval.yml`.
  The live pipeline remains optional/provider-dependent and is not deterministic language-quality
  proof.
- Existing architecture, deployment, CI/supply-chain, performance, recovery, and runbook documents
  will be linked/refined rather than duplicated.

## Final-gap matrix

| Area | Current evidence | Final required proof | Known limitation | T21 action |
| --- | --- | --- | --- | --- |
| Application | T12 runtime, T13 gateway, T14 policy, focused stage suites | One full backend regression and import/runtime integration | OpenAI/Celery timing sensitivities | Run once; classify exact signatures only |
| Frontend | T15 transport and T16 console unit/E2E evidence | Locked install, format, lint, typecheck, Vitest, build, meaningful E2E | No large browser matrix | Run canonical final gates once |
| Database/migrations | T07 RLS and T20 restore evidence | Single immutable head and migration-chain integrity | No new migration expected | Verify head `e9f0a1b2c3d4` |
| Tenant isolation | T07 RLS plus T20 fresh-restore tenant counts | Final route/static checks and deterministic namespace contract | Offline eval does not replace real PostgreSQL RLS proof | Cite T07/T20 and add bounded eval scenario |
| Auth/security | T09/T10/T15/T19 evidence | Final bounded token/CORS/secret/workflow/static scan | Hosted PR path not yet executed | Re-run focused static/runtime checks |
| Conversation runtime | T12 durability/cancellation/idempotency evidence | Terminal uniqueness and invalid-transition regression | Deployed SSE fixture remains unavailable | Add deterministic state-machine eval cases |
| Model gateway/failure policy | T13/T14 tests and T20 Mock-provider fault evidence | Deterministic fallback/degradation metrics | No live-model quality claim | Exercise actual gateway/policy with scripted adapters |
| Observability | T17 real API/outbox/worker correlation and sink-outage evidence | Architecture/evidence links remain coherent | No external production telemetry backend | Audit docs and cite measured evidence |
| CI/supply chain | T18 workflows, scans, SBOM, local image proof | Final graph/permission review and hosted check-name plan | Hosted PR/GHCR/attestation proof deferred | Audit workflows; document post-PR proofs |
| Helm/k3s | T19 disposable install/upgrade/rollback and static validation | Final static lint/template/schema/kubeconform/ShellCheck | No real VM/DNS/public CA or live AWS | Run static-only final checks |
| Performance/recovery | T20 load, failures, backup, fresh restore, DR | Preserve measured results and limitations | No PITR, object-store recovery, long soak, production capacity | Cite evidence; do not rerun full T19/T20 drills |
| AI evaluation | Existing provider-dependent pipeline and unit suites | One provider-free dataset, command, objective assertions, metrics | Deterministic checks do not measure linguistic quality | Extend `app/evaluation` and `eval.yml` |
| Documentation | Distributed architecture/guides/runbooks | One coherent current entry point and final system view | Historical ledgers remain append-only | Fix current-state drift and add links |
| Portfolio/interview | Stage plans contain strong but scattered evidence | Case study, claim/evidence index, and interview guide | No fabricated production experience | Create concise evidence-backed documents |

## Implementation plan

1. Reconcile accepted task-plan locations and current state without rewriting historical evidence.
2. Add a compact synthetic deterministic workflow dataset and runner inside the existing evaluation
   package; add focused tests and the canonical CI command.
3. Add/refine the final architecture view, case study, evidence index, interview guide, known
   limitations, README entry points, and prepared final PR body.
4. Run the offline evaluation and focused tests, then perform the one canonical full backend suite.
5. Run backend static/migration/route/security gates, locked frontend gates and E2E, a clean-context
   Docker build/smoke, and final Helm static validation.
6. Record exact evidence, commit T21 work separately, push normally, and leave T21
   `IN_PROGRESS / VERIFY_PENDING` with no PR or merge.

## Preserved limitations

- OpenAI SDK cold-start deadline sensitivity.
- Celery fresh-process import deadline sensitivity.
- Visible dependency/image vulnerability baseline.
- Hosted protected-PR, GHCR publication, and provenance/attestation proof not yet executed.
- Real public VM, DNS, and public-CA proof not executed.
- Object-storage runtime recovery not exercised; demo `emptyDir` is not production-safe.
- PITR and live AWS deployment not implemented.
- Long soak and production capacity not claimed.

## Implementation evidence and blocker

- Deterministic dataset: 12 synthetic scenarios, 12 passed, 0 failed. Routing 4/4, tool routing
  3/3, approval 1/1, authorization 1/1, tenant isolation 1/1, fallback policy 2/2, and terminal
  uniqueness 3/3 passed without a real provider or network.
- Focused evaluation tests: 3 passed. Focused Ruff and `ty` passed. Canonical backend Ruff and `ty`
  also passed; Ruff found and normalized one line-ending-only package file.
- Fresh disposable migration database reached the single `e9f0a1b2c3d4` head.
- The first canonical full backend attempt collected 1,848 tests and stopped at 5% after
  `localhost` PostgreSQL timeouts. Direct `127.0.0.1` connectivity passed; classification is
  `TEST_ENVIRONMENT` and that attempt is not final evidence.
- After 3/3 corrected DB preflight tests passed, the explicitly authorized replacement full run
  completed exactly once: 1,848 collected, 1,810 passed, 1 failed, 0 errors, 37 skipped, 81.93%
  coverage, and 2,231.21 seconds (`37:11`).
- The sole failure was `tests/test_chat_api.py::test_chat_timeout_after_answer_closes_without_error`.
  A focused diagnostic reproduced it in 5.84 seconds: the timeout error was emitted before the
  expected streamed token. It is not an OpenAI/Celery baseline or a DB environment failure;
  classification is `FEATURE_REGRESSION`. T21 did not modify `app/api/v1/chat.py`, and no repair was
  made.
- Per instruction, final readiness stopped at this regression. Frontend, Docker, final Helm static,
  route/security, final CI, and PR-readiness closeout are not claimed. T21 remains
  `IN_PROGRESS / IMPLEMENT_BLOCKED`.
