# V1.2-INTEGRATION-PR-FIX

Status: `AWAITING_ACCEPTANCE / HOSTED_CHECKS_PENDING`

## Starting state

- Branch: `chore/v1.2-bootstrap-docs`; clean at `f4471fcf76ab1e14d9d0aa3a8cb69a1b3ffadb4a`.
- `git fetch origin --prune` completed; `origin/main` matches the required
  `0a502933dd3502c97bfff72f66bad89a84735d08`.
- PR #15 is open. Hosted Backend quality has a stale expected Alembic head; Backend tests fail
  because one bootstrap test asserts global shared-table counts after 148 users already exist.
- Current accepted migration head is `f0a1b2c3d4e5`, downstream of `e9f0a1b2c3d4`.

## Fix boundary

- Keep CI's exactly-one-head check and update its expected revision.
- Reproduce the test failure with an unrelated user, then scope every bootstrap assertion to
  stable dataset identifiers and ownership relationships.
- Change no production application, migration, frontend, dependency, or README file.

## Local verification

- The polluted-state test failed before the assertion repair (`3` users versus global expectation
  `2`) and passed afterward. An unrelated user is persisted inside the test transaction.
- `uv lock --check`: pass. `uv run alembic heads`: exactly `f0a1b2c3d4e5 (head)`.
- Migration-chain tests: `12 passed`. Bootstrap directory: `7 passed`.
- Bounded auth/tenant, order/refund, knowledge/bootstrap selection: `48 passed, 1 deselected`,
  with the bootstrap regression last. The initial run had `48 passed, 1 error`: one unrelated
  tenant API case could not resolve the local Redis hostname during fixture setup. No test harness
  or production change was made for that environment issue.
- `uv run ruff check app tests`, `uv run ruff format --check app tests`, and
  `uv run ty check --error-on-warning app tests`: pass.

## Remaining gate

- Create one focused fix commit and push normally to PR #15.
- Verify the five new-head protected checks, supply-chain jobs, and extended check statuses.
- Leave PR #15 open and unmerged for external acceptance.
