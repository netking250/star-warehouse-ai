# T16 - Enterprise Console

## Objective

Extend the existing React administration application into one capability-aware enterprise
console for the accepted backend surfaces. Preserve the existing admin routes and components,
use the T15 browser transport boundary, and keep backend authorization authoritative.

## Recovery Evidence

- User-directed acceptance state: `T14 = PASS_WITH_NOTES`, `T15 = PASS`, `T16 = NOT_STARTED`,
  `T17 = NOT_STARTED`.
- Branch: `feat/t14-t21-enterprise-hardening`.
- Recovery HEAD: `828caea`.
- Working tree was clean at recovery and the branch tracks
  `origin/feat/t14-t21-enterprise-hardening`.
- No T16 active plan existed. The accepted ADR-018 scope names the Enterprise Console domains
  Overview, AI, Operations, Security, and Compliance; the roadmap and decisions contain no
  narrower page list.
- The two protected-main baseline debts remain deferred: OpenAI SDK cold-start deadline
  sensitivity and Celery fresh-process import deadline sensitivity.

## Accepted Contracts Read

- Root and admin-frontend `AGENTS.md` guidance.
- Architecture guardrails and architecture explanation index.
- T09 Authorization: `/me` scopes are compatibility data for current local authority; the
  frontend may use them for visibility only and every protected backend route remains authoritative.
- T10 Secure Browser Session: HttpOnly cookie, in-memory CSRF, exact Origin, server logout, and
  cookie/origin WebSocket transport.
- T11 Compliance Lifecycle: immutable audit evidence, exact expiring export approvals, separation
  of duties, and bounded retention operations.
- T12 Conversation Runtime: read-only runtime state is exposed through accepted conversation
  routes; no arbitrary state mutation is permitted.
- T13 Model Gateway and T14 Failure Policy: provider routing, retry, fallback, and circuit policy
  remain backend-owned; no provider secret or breaker override UI is added.
- T15 Frontend Transport: all authenticated page data and mutations use `apiFetch`/`apiFetchJson`,
  normalized `TransportError`, cookie session, CSRF, and no browser token paths.

## Scope Recovery Matrix

| Feature | Backend API exists? | Frontend exists? | T16 action |
| --- | --- | --- | --- |
| Overview | Yes, existing task/metric/alert/approval reads | Partial dashboard tabs | Add a lightweight status-first overview using bounded existing reads. |
| AI configuration | Yes, agent config, versions, routing, evaluation APIs | Existing `AgentConfig` | Refine and route through the shared shell; hide manage controls without `operations.manage`. |
| Operations/review/conversations | Yes, existing task, alert, metric, conversation reads and accepted review mutations | Partial dashboard tabs/components | Add one operations workspace and preserve existing component routes. |
| Security/access | Yes, memberships, role and active-status endpoints | None | Add capability-aware membership page with safe role/status mutations. |
| Compliance/approvals | Yes, approval list/decision and bounded retention endpoints | Export request exists; no approval console | Add approval queue and bounded retention controls with confirmation. |
| Notifications | WebSocket notifications only; no durable notification API | Ephemeral existing toast | Keep existing socket/toast behavior; no second persistence model. |
| General audit list/detail | No safe general list/detail API; only scoped config audit and compliance evidence | None | Backend gap; do not invent a route or raw payload viewer. |
| Async jobs/outbox operations | No operator list/retry/cancel contract | None | Backend gap; do not call Celery or mutate outbox state directly. |
| Provider/model/circuit administration | No safe status/config/secret-management API | None | Backend gap; keep AI provider administration out of T16. |
| Tenant switching | No accepted privileged cross-tenant contract | None | Exclude; retain resolver-bound current tenant only. |

## Information Architecture

- `/` — Overview.
- `/operations` — operations status, review queue, and conversation metadata where the current
  operator has the corresponding capabilities.
- `/ai` — existing agent configuration/evaluation surface under the shared console shell.
- `/security` — current-tenant membership and effective capabilities.
- `/compliance` — sensitive-export approval metadata and bounded retention operations.
- Existing `/knowledge`, `/feedback`, and `/metrics` routes remain stable and are attached to the
  same single shell; no duplicate admin navigation tree is introduced.

## Implementation Constraints

- Use current `/me` scopes for UX visibility only; never use username, email, JWT role claims, or
  `is_admin` as authority.
- Keep sensitive values out of the UI: passwords, tokens, session secrets, API keys, provider
  credentials, and raw sensitive export content are never rendered.
- Use server pagination and existing query parameters only. No unbounded client fetches or
  arbitrary polling is introduced; operational refresh is manual/bounded.
- Use one confirmation dialog pattern for role/status changes, approval decisions, retention
  execution, and existing high-impact actions touched by T16.
- On 401, clear the browser session and privileged cache. On 403, preserve authentication, remove
  stale privileged cache, and reconcile current `/me` capabilities.
- No backend route, schema, migration, provider secret storage, T17 observability work, or
  deployment control is in scope.

## Verification Plan

- Add focused Vitest coverage for capability guards, cache clearing on logout/permission denial,
  safe mutation confirmation/double-submit handling, and the new query helpers.
- Add focused Chromium E2E coverage for authorized console access, unauthorized denial, a read-only
  operations flow, a compliance decision flow, and logout removal of privileged UI.
- Run frontend format, lint, typecheck/build, and unit tests; run only focused backend
  authorization/compliance/runtime/route guards and the T15 transport security guards.
- Run route inventory and `alembic heads`; no migration is expected.

## Start Status

- T14: `PASS_WITH_NOTES`.
- T15: `PASS` by explicit user acceptance instruction; prior roadmap `AWAITING_ACCEPTANCE` text is
  retained as historical evidence and superseded in the current state ledger.
- T16: `IN_PROGRESS / VERIFY_PENDING`.
- T17: `NOT_STARTED`.

## Implementation Closeout

- Added one shared `AdminLayout` hierarchy with capability-aware navigation, session/direct-route
  guards, explicit access-denied presentation, and stable existing workspace routes.
- Added Overview, Operations, Security, and Compliance pages over existing API contracts; routed
  the existing AI config page into the shell and gated its mutations by `operations.manage`.
- Added safe metadata-only review/conversation/approval tables, server pagination/filtering where
  supported, manual refresh, bounded retention preview/execute, shared confirmation dialogs, and
  normalized `TransportError` presentation.
- Added cache clearing/reconciliation for 401, 403, logout, and server-derived capability changes;
  active 403 queries stay in explicit error state rather than being removed and refetched in a
  loop.
- Added focused capability/error unit tests and deterministic Chromium coverage for authorized
  access, capability denial, backend 403, read-only operations, approval mutation, CSRF, logout,
  and the existing customer browser-session flow.
- Verification: frontend format check, lint, build, Vitest `51 passed`, focused T15 guards `31
  passed`, and Chromium `6 passed`. Backend pytest was attempted but PostgreSQL host `db` was not
  resolvable during shared setup; direct route inventory reported `0` unclassified routes and
  Alembic reported `e9f0a1b2c3d4` as the single head.
- New T16 debt: environment-only inability to run database-backed focused backend tests because
  PostgreSQL/Docker was unavailable. No feature debt or migration was added.
