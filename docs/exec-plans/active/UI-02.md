# UI-02 — V1.1 Admin Enterprise Console Visual Redesign

Status: `AWAITING_ACCEPTANCE`

Execution stage: `EXTERNAL_ACCEPTANCE_PENDING`

## Scope

- Continue from accepted UI-01 head `5c70565db2985fa943314a3f69d72948ec65dee8` on
  `feat/ui-v1.1-enterprise-visual`.
- Redesign the eight active Admin routes using the accepted semantic design system and shell.
- Preserve all hooks, API contracts, capability gates, mutations, auth/session behavior, and real
  data contracts; add no fake operational data or runtime dependency.
- Add a focused visual E2E matrix covering light/dark desktop and selected 1024px views.

## Implementation

- Consolidated reusable page headers, metric cards, status badges, panels, filters, tables, and
  state presentation without duplicating shadcn/Radix primitives.
- Applied a stable hierarchy and responsive information density to Overview, Operations, AI
  Runtime, Security, Compliance, Knowledge, Feedback, and Metrics while preserving their hooks and
  actions.
- Removed the temporary legacy Admin theme bridge after active page interiors migrated to semantic
  tokens; Grafana now follows the active theme.

## Verification

- `npm --prefix frontend ci`
- `npm --prefix frontend run format:check`
- `npm --prefix frontend run lint`
- `npm --prefix frontend run test`
- `npm --prefix frontend run build`
- `npm --prefix frontend run test:e2e`
- Review light/dark screenshots for all active routes and 1024px screenshots for Overview,
  Operations, and Knowledge outside Git.

Results: clean install passed; Prettier and ESLint passed; 14 unit files / 58 tests passed; the
production build transformed 1929 modules; all 9 Playwright tests passed. The visual matrix
captured all eight routes in both themes at 1440px and three required routes at 1024px with no
browser console errors or unexpected HTTP 4xx/5xx.

## Boundaries

- No backend, migration, workflow, API, auth, authorization, tenant, Agent/RAG, or business change.
- No customer redesign, heavy animation, fake data, new runtime dependency, PR, or merge.
- UI-03 must not start before UI-02 external acceptance.
