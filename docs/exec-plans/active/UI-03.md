# UI-03 - V1.1 Premium Customer AI Experience

Status: `AWAITING_ACCEPTANCE`

Execution stage: `EXTERNAL_ACCEPTANCE_PENDING`

## Scope

- Continue from accepted UI-02 head `d1500077432fd6dab1719a027298fb5973b0f146` on
  `feat/ui-v1.1-enterprise-visual`.
- Refine the Customer login, auth initialization, shell, sidebar, empty state, messages, composer,
  feedback, notifications, and mobile presentation using the accepted V1.1 foundation.
- Preserve authentication, cookie session, CSRF, SSE, WebSocket, conversation, cancellation,
  feedback, tenant, Agent/RAG, and business contracts.
- Remove unsupported claims and non-functional decorative controls; add no fake business data or
  runtime dependency.

## Plan

1. Audit the complete Customer component, hook, shared-theme, shared-shell, and E2E hierarchy.
2. Implement the visual refinement without moving transport or domain behavior into components.
3. Add focused presentation-behavior tests and a deterministic Customer visual E2E matrix.
4. Run clean install, format, lint, unit, build, and Playwright verification; inspect screenshots.
5. Record evidence, set UI-03 to `AWAITING_ACCEPTANCE`, commit once, and push normally.

## Verification

- `npm --prefix frontend ci`
- `npm --prefix frontend run format:check`
- `npm --prefix frontend run lint`
- `npm --prefix frontend run test`
- `npm --prefix frontend run build`
- `npm --prefix frontend run test:e2e`
- Review required light/dark desktop and 390px mobile screenshots outside Git.

Results: clean install passed; Prettier and ESLint passed; 15 unit files / 62 tests passed; the
production build transformed 1929 modules; all 12 Playwright tests passed. The Customer visual
matrix captured Light/Dark login, empty chat, active and long-answer conversations, expanded
feedback, 1280/1024 desktop, and 390/360px mobile states. It found no browser console errors,
unexpected HTTP 4xx/5xx, or horizontal overflow. Screenshots remain ignored test artifacts.

## Boundaries

- No backend, migration, workflow, API, auth, authorization, tenant, Agent/RAG, or business change.
- No new animation, Markdown, chat UI, state-management, or design-system dependency.
- No Admin redesign, UI-04 work, PR, or merge.
