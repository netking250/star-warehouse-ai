# M02 - Git Main Consolidation & Branch Cleanup

## Objective

Safely place all accepted T00-T13 project work and legitimate remote-main history on canonical
`main`, modernize repository entry documentation, and remove only branch refs whose work is either
reachable from `main` or explicitly inspected and rejected as obsolete/out of scope.

## Recovery Evidence

- Branch: `main`; recovery HEAD: `bf0d5b9`; initial `main == origin/main`.
- Working tree: extensive coherent T00-T13/M01 implementation, verification, migration, test, and
  documentation work recorded by the project execution ledger; ignored local `.env`, caches,
  coverage, uploads, and dependency/build outputs are excluded.
- External gate supplied by the maintenance request: `T13 = PASS`; T14 remains `NOT_STARTED`.
- GitHub remote default branch reported by `git remote show origin`: `main`.
- Local branches: `main` only.

## Initial Remote Branch Inventory

| Branch | Tip | Main-only / branch-only | Decision before mutation |
| --- | --- | ---: | --- |
| `origin/main` | `bf0d5b9fe756` | `0 / 0` | Preserve as canonical history |
| `origin/feat/star-warehouse-ai-v2` | `d15cc0a6e100` | `8 / 0` | Fully contained; delete after push |
| `origin/feat/star-warehouse-ai-v3` | `9625a9eab27d` | `6 / 0` | Fully contained; delete after push |
| `origin/fix/star-warehouse-ai-v3-1` | `628cc8ac8e4f` | `5 / 0` | Fully contained; delete after push |
| `origin/fix/alertmanager-config` | `b9f1a91079cd` | `0 / 1` | Legitimate one-line fix; integrate normally |
| `origin/dependabot/github_actions/workflow-actions-8d9caf0e14` | `47c8d9731202` | `0 / 1` | Bot dependency upgrade; reject as out of M02 scope |
| `origin/dependabot/npm_and_yarn/frontend/frontend-dependencies-b1495ccbf6` | `d7d69406a34e` | `0 / 1` | Bot dependency upgrade; reject as out of M02 scope |
| `origin/dependabot/pip/python-dependencies-498c536cf0` | `2eb9f5be9676` | `0 / 1` | Bot dependency upgrade; reject as out of M02 scope |

## Safety Findings

- No branch-history ambiguity: only the Alertmanager branch contains accepted in-scope work not
  already reachable from `main`.
- No high-confidence real credential was found in files eligible for commit. Matches were local
  documented defaults or deliberate fake keys in hermetic policy tests; ignored `.env` is not
  eligible for commit.
- The three Dependabot commits are dependency upgrades from the old `main` base. M02 is explicitly
  prohibited from upgrading dependencies and will not merge them.
- Before integration or deletion, create a local snapshot tag covering the complete accepted
  working tree and an OS-temp Git bundle containing every current ref.

## Execution Plan

1. Update README and execution state without changing application architecture.
2. Create a local snapshot commit/tag through an alternate index, then create an external bundle.
3. Commit the accepted T00-T13/M01 working tree on `main` and merge the Alertmanager fix normally.
4. Run link, identity, Git hygiene, configuration, static, and targeted Alertmanager checks.
5. Fetch immediately before a normal non-force push; stop if `origin/main` moved.
6. Verify local/remote synchronization, then delete only inspected obsolete remote branches.
7. Prune, verify default branch/topology/lost-work guards, and leave M02 `AWAITING_ACCEPTANCE`.

## Execution Result

- Safety snapshot tag: `repo-consolidation-backup-20260914-201849` at
  `65a30ae5cd394e6672fb802378eadf7568f52109`; the tag was not pushed.
- Verified bundle: `C:\Users\11\AppData\Local\Temp\star-warehouse-pre-consolidation-20260914-201849.bundle`
  (`4,614,706` bytes), containing all pre-cleanup local and remote refs.
- Accepted working tree committed as `620b82f`; the Alertmanager branch was integrated by normal
  conflict-free merge as `c77987b`. Original `origin/main` history remains the merge base.
- Validation passed: project identity/local links, both Compose files, Alertmanager `amtool`, Ruff,
  Ruff format, ty, lock consistency, Alembic single head `e9f0a1b2c3d4`, `git diff --check`, and the
  focused model-gateway suite (`32 passed`). One initial cold-start fake-transport timeout passed on
  exact and full focused reruns.
- Immediate pre-push fetch confirmed `origin/main` was unchanged at `bf0d5b9fe756`. The normal,
  non-force push was rejected by GH006: changes require a pull request, five checks are required,
  and merge commits are prohibited.
- Per the maintenance STOP rule, no remote or local branch was deleted and no protection setting
  was changed.
- Recommended continuation: create `repo-consolidation-pr` from
  `origin/fix/alertmanager-config` (`b9f1a91`), cherry-pick the accepted snapshot commit
  `620b82f`, resolve only the already-understood Alertmanager overlap if Git requests it, push that
  linear branch, and open a PR into `main`. After checks and PR merge, fetch/verify `origin/main`
  before any branch deletion.

## PR Fix Recovery

- Recovery revalidated clean local `main` at `c5a4a1aa8be5e7e3ea2849507fef1d7c3d56d928`,
  unchanged `origin/main` at `bf0d5b9fe756fb1527c605120b03a0d6531471cd`, and backup tag
  `repo-consolidation-backup-20260914-201849` at the expected `65a30ae5cd394e6672fb802378eadf7568f52109`.
- The external Git bundle still exists at the recorded path and size and was not modified.
- The only merge in the rejected local range is `c77987b`; missing legitimate content is not the
  cause of rejection.
- `b9f1a91` is a direct child of protected `origin/main`. A new normal commit was constructed with
  `b9f1a91` as parent and canonical tree `b3a03fd2e7cd3e0a250ea9f7ff6e054e2a6808f8`.
- Initial `repo-consolidation-pr` commit `02a91d4` has zero merge commits, descends from
  `origin/main`, and is tree-identical to canonical local `main`. Subsequent PR-fix documentation
  is intentionally limited to project execution state.

## Status

`IN_PROGRESS` — `PR_FIX`.
